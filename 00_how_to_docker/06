# Launch a container, set restart varible, bind port 80 to docker-machine port 5000, (-d) detach,
# (--name) name it overlord and use the nginx image

# Use <docker-machine-ip>:<port> to test
 

docker run --restart=always -p 5000:80 -d --name overlord nginx
