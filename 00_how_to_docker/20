docker swarm init --advertise-addr $(Char docker-machine ip):2377

Use the Char docker-machine's ip address in above command. Only use port 2377 or leave it blank to use default(2377).

To test:
``` sh
docker node ls
```
