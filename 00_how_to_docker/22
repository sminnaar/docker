# ssh into the container:

docker-machine ssh Aiur

# Join a docker swarm using a docker swarm token and host IP.

# Run this command in the container, replacing everything within the <>:

docker swarm join --token <swarm join-token> <docker-machine ip Char>:2377
