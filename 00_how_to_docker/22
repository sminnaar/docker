# ssh into the container:

``` sh
docker-machine ssh Aiur
```

# Join a docker swarm using a docker swarm token and host IP. The first
# $() will return the worker join-token. (-q) quite, only return token
# Run this command in the container

``` sh
docker swarm join --token $(docker swarm join-token worker -q) $(docker-machine ip Char):2377
```
